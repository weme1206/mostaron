# 更新日志

## v1.4.3 (2026)
- 修复：输入框 AI 改写按钮在输入消息后不出现的问题（输入变化时未刷新按钮显隐）。
- 新增：用图片（PNG/WebP）导入角色卡时，自动用该图片作为角色头像。
- 优化：版本 1.4.2 → 1.4.3。

## v1.4.2 (2026)
- 新增：主页角色/群聊卡片右上角显示最新消息时间（当日显示时间，否则显示日期）。
- 调整：输入框"正在改写…"改为占位提示（hint），不再写入输入内容；AI 改写按钮移到输入框**左上角**（小尺寸），角色/群聊位置一致。
- 修复：编辑群聊误克隆成新群聊的问题（编辑保留原 id）。
- 调整：角色/群聊克隆（任意模式）名字都加"副本"，新角色出现在主页**最上方**（不置顶），导入角色卡也置顶。
- 调整：导入角色卡图标改为下箭头，导出角色卡图标改为上箭头。
- 优化：使用说明更新；版本 1.4.1 → 1.4.2。

## v1.4.1 (2026)
- 优化：查找聊天记录结果按**最近优先**排序；定位消息后按返回键回到查找结果页（查找页压栈）。
- 调整：角色卡**导入**移到主页右上角"+"菜单（创建群聊下方），**导出**移到长按角色的菜单；导出支持 JSON / PNG 两种。
- 调整：群聊记忆模式改为**创建时选择**（弹窗3选1），建好后不可改；长记忆页小字显示当前模式。
- 新增：克隆群聊可**改记忆模式克隆**（克隆设置、聊天与记忆全新）。
- 调整：**继续说**不再插入用户提示消息，角色直接续写。
- 新增：输入框**AI 改写**按钮（含改写中/撤销/取消逻辑，改写后不发送、不自动回复）。
- 优化：版本号 1.4.0 → 1.4.1；使用说明更新。

## v1.4.0 (2026)
- 新增：**SillyTavern 角色卡导入/导出**（PNG / WebP / JSON，V1/V2），角色卡带世界书会自动加入世界书库；角色卡扩展字段（场景、示例对话、作者注释、系统提示、备选开场白、标签等）；文字一键生成角色也会生成这些字段。
- 新增：**群聊记忆模式**（全新整体记忆 / 角色独立全新记忆 / 角色独立同步记忆）。
- 新增：**查找聊天记录**（角色 + 群聊），命中词主题色高亮，点击跳转到消息位置，可返回查找结果。
- 新增：**删除从这里开始的消息**（长按消息），并同步清除产生的长记忆。
- 新增：**克隆角色/群聊**（全新 vs 携带聊天与记忆），克隆保留全部设置，名字加"副本"。
- 新增：**继续说**按钮 + 操作区改为纯图标；用户编辑/AI 改写后自动回复。
- 新增：**主设置字体大小**调节。
- 优化：版本号 1.3.0 → 1.4.0；数据库升级到 v8；使用说明末尾附仓库地址。

## v1.3.0 (2026)
- 重构：世界书改为**全局管理**（“设置 → 世界书”里增删改），每个角色/群聊在编辑里**勾选启用哪些**（默认全不启用）。
- 优化：全新世界书选择界面（底部弹出勾选）；旧“按角色单独存”的世界书条目在升级后废弃。

## v1.1.7 (2026)
- 新增：主题色/气泡色**调色盘**选择，可保存/删除。
- 新增：**头像裁剪**（缩放平移 + 固定圆 + 保存）。
- 新增：**按角色/群聊独立配置世界书**。
- 新增：**AI 手动/自动生成记忆**；记忆优化为"置顶 AGENTS.md + 最近记忆"双注入。
- 优化：气泡下方操作按钮与气泡对齐；主页标题真正居中；回车换行；进入聊天自动到底部；设置优先级高于主设置；上传背景后顶部顶栏透明；合并导入不覆盖已有数据；新建角色/群聊置顶。

## v1.1.0
- 首个完整功能版：BYOK 多模型、角色卡、群聊（三种回复模式）、长期记忆、世界书、主题、导入导出、自定义签名。

## v1.0.0
- 初始搭建（Flutter 工程 + 构建管线）。
