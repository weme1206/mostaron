# 更新日志

## v1.3.0 (2026)
- 重构：世界书改为**全局管理**（“设置 → 世界书”里增删改），每个角色/群聊在编辑里**勾选启用哪些**（默认全不启用）。
- 优化：全新世界书选择界面（底部弹出勾选）；旧“按角色单独存”的世界书条目在升级后废弃。

## v1.1.7 (2026)
- 新增：主题色/气泡色**调色盘**选择，可保存/删除。
- 新增：**头像裁剪**（缩放平移 + 固定圆 + 保存）。
- 新增：**按角色/群聊独立配置世界书**。
- 新增：**AI 手动/自动生成记忆**；记忆优化为"置顶 AGENTS.md + 最近记忆"双注入。
- 优化：气泡下方操作按钮与气泡对齐；主页标题真正居中；回车换行；进入聊天自动到底部；设置优先级高于主设置；上传背景后顶部顶栏透明；合并导入不覆盖已有数据；新建角色/群聊置顶。

## v1.1.0
- 首个完整功能版：BYOK 多模型、角色卡、群聊（三种回复模式）、长期记忆、世界书、主题、导入导出、自定义签名。

## v1.0.0
- 初始搭建（Flutter 工程 + 构建管线）。
